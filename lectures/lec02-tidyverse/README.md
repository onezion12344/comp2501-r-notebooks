# Lec 2 — tidyverse

> **status: coming soon** — placeholder. Will be built from `Lec02-R markdown, Tidyverse, Data import.pdf`.

## Planned content

- R Markdown workflow (knitting, chunk options)
- tidyverse: the `readr`/`dplyr`/`tidyr` toolchain
- Importing data: `read_csv`, `read_tsv`, data types on import
- More dplyr: `join`, `pivot_wider`/`pivot_longer`, `rename`, `case_when`

## Open in Colab

Link will go here once the notebook is ready.

## Files

| File | Purpose |
|------|---------|
| `lec02_tidyverse.ipynb` | (to be created) |

## Build it with me

If you want this notebook now, tell me (or drop the Lec02 PDF into the course folder and I'll read it) and I'll generate the interactive version.
